import { initializeApp } from "firebase/app";

const firebaseConfig = {
  apiKey: "temp",
  authDomain: "temp",
  projectId: "temp",
};

const app = initializeApp(firebaseConfig);

export default app;